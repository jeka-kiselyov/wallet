<?php

class controller_index extends userside_controller
{
	function index()
	{
 	}

 	// function saveentityexample()
 	// {
	 //    $form_checker = new checker;
	 //    if ($this->is_post())
	 //    { 
	 //      $invite_request = new invite_request;
	 //      $invite_request->fill($form_checker->post());
	 //      $invite_request = $form_checker->save_entity($invite_request);
	 //      if (isset($_POST['ajax']) && $_POST['ajax'])
	 //      {
	 //        if ($form_checker->is_entity_saved())
	 //          $this->json_results(array('status'=>'success'));
	 //        else
	 //          $this->json_results(array('status'=>'error', 'errors'=>$form_checker->get_errors()));
	 //      }
	 //    }
	 //    $this->ta('form_checker', $form_checker);
 	// }
}